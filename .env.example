# Database Configuration (PostgreSQL for Render)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_postgres_password
DB_NAME=blog

# Backend Server
PORT=4000
NODE_ENV=development

# JWT & Security
JWT_SECRET=generate_a_strong_random_secret_key_here_use_openssl_rand_base64_32
ADMIN_PASSWORD=Admin@123

# Frontend URL
FRONTEND_URL=http://localhost

# Cookie Security
COOKIE_SECURE=false

# Production (Render)
# For Render deployment, set:
# NODE_ENV=production
# COOKIE_SECURE=true
# FRONTEND_URL=https://blog-frontend-xyz.onrender.com
# DB_HOST=your-postgres-host-from-render.c.render.com
# DB_PASSWORD=your-postgres-password-from-render
